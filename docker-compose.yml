services:
  mcp-airbnb:
    build:
      context: .
      dockerfile: Dockerfile
    image: mcp-airbnb:latest
    container_name: mcp-airbnb
    ports:
      - "3041:3041"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
